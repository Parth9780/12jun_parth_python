/*
    if-else is candition control 
    if is ckeck the condition.
*/

#include<stdio.h>
#include<conio.h>
int main()
{
    int a, b;
    char c;
    printf("Enter the value of a: ");
    scanf("%d",&a);
    if (a >= 100){
        printf("a is a three no value");
    }
    else{
        printf("a is a two no value");
    }
    return 0;
}